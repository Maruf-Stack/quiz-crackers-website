import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Quiz = () => {
    // const startQuiz = useLoaderData();
    const startQuiz = useLoaderData();
    return (
        <div>
            This is quiz
        </div>
    );
};

export default Quiz;